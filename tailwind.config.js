/** @type {import('tailwindcss').Config} */
export const content = ['./src/**/*.{js,jsx,ts,tsx}'];
export const theme = {
  extend: {
    colors: {
      gray: {
        custom: '#EEEEEE',
      },
    },
    spacing: {
      22: '22%',
    },
  },
};
export const plugins = [];
